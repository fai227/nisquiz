# Linux操作
```
sudo systemctl restart quiz
sudo systemctl status quiz
```

# ファイル配置
- quizフォルダ下
  - 問題集(ジオゲッサー).csv, 問題集(入れ替えクイズ).csv, 問題集(択一クイズ).csv
  - Excel上で作成し格納
- dataフォルダ下
  - user.txt
  - ユーザー名を行ごとに入れておく